import React, { Fragment } from "react";
import Products from "./Products";

const Store = (props) => {
    return (
        <Fragment>
            <Products />
        </Fragment>
    )
}

export default Store;